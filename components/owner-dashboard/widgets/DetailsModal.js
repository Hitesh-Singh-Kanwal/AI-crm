// Relocated to components/shared/DetailsModal.js so the main-dashboard
// widgets (components/dashboard/widgets/) can share it too. Re-exported here
// so existing imports in this folder keep working unchanged.
export { default, PAGE_SIZE } from '@/components/shared/DetailsModal'
