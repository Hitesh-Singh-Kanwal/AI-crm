NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Google Analytics (GA4 Data API) for live form views
# You must create a Google Cloud service account with access to the GA4 property.
# Store private key with literal "\n" newlines (it will be normalized at runtime).
GA4_PROPERTY_ID=
GA4_CLIENT_EMAIL=
GA4_PRIVATE_KEY=

